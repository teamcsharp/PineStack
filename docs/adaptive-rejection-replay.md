The actual version-6 grader was replayed against all 47 tint occurrences in the frozen audit, with the recorded words, force, style chunks, caller context and prior-line context. Learner mode changed only in a temporary data directory, first to strict permission and then fluid. Model calls and review writes were forbidden. Full old, raw and fluid reports are retained in [adaptive-rejection-replay.json](adaptive-rejection-replay.json).

Four occurrences now pass the raw grade. With the final bounded guards, fluid mode permits 13 occurrences across seven exact wording pairs: three raw passes and ten explicit style advisories. Thirteen occurrences gain rhyme evidence and none lose it. All eight stored semantic failures remain blocked; 28 still lack sufficient rap evidence. These counts describe previously rejected material, not the station's overall acceptance rate.

The initial replay admitted 16 occurrences across ten pairs; all ten were read. Two sense/dense examples remained unfinished and incoherent; one pricing variant dropped “try”, changing an attempt into an accomplished action. Those three pairs are now blocked by narrow fluid-permission guards, with the exact source/candidate artifacts preserved in `editorial.guard_evidence`. A fourth already-blocked unfinished example gains the same source-needs-repair explanation. The raw machine grades remain unchanged.

The remaining seven eligible pairs retain the main proposition while allowing the requested style flexibility. Five price/touch/clutch variants remain awkward metaphors or near copies with rhyme padding. The kid/bid example retains the complaint but adds awkward shorthand. The station-location example is tautological. Eligibility does not certify polished writing. All 30 acceptance/guard/integration tests pass, including preserved attempts, modal wording, noun uses of “try”, ordinary complete endings and strict-mode behavior.

The frozen vocabulary contains 758 content words from retained style chunks, rather than the unsaved complete live crystal vocabulary. Sources, snapshot and preview judge state were unchanged. The new attempt guard is a narrow lexical check, not a full predicate parser; the orphan guard uses the retained unfinished fragment and an ellipsis completion rather than an adjective blacklist. No candidate was rewritten, approved, queued, recorded or played.
