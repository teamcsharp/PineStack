The consistent snapshot through event 3537 contains 48 new rejection events since process start 1788805392.02: 47 tint events and one unfinished-draft event. These represent 38 distinct review IDs, 42 failed rewrite attempts and six cuts. They are not 48 discarded programmes or a failure rate over all generated material. Full exact text, evaluations, event identities and the snapshot hash are in [adaptive-rejection-audit.json](adaptive-rejection-audit.json).

There are no new blend events in this window and no literal “Bield” in the new wording. The latest blend refusal, event 3489, occurred about 16 seconds before this process. Its proposal dropped much of the assembled host conversation and moved the tail. The refusal protected the original: `blend_script` returns the retained original when the proposal fails. The current queue includes 17 older blend reviews among 1,656 pending records; all history contains 3,537 events across 1,663 records.

Among the 47 tint events, 41 report missing rhyme, 22 insufficient transformation and eight a semantic mismatch; reasons overlap. Thirty-nine pass the stored semantic checks and six pass the stored rap check. Two concrete detector defects are reproducible:

- Events 3494, 3499 and 3526 contain touch/clutch. The spelling approximation puts their vowels in different classes although the ending sounds match. The added “excessive clutch” wording remains awkward; correcting this detector does not certify the entire sentence.
- Event 3497 contains kid/bid, but the three-word clause “A disaster bid” gets merged into the following clause by a four-word minimum, hiding that endpoint. The source and original speaker order remain in the retained evidence.

Events 3505/3506/3522 also expose weak evidence around eight/fate/mistake. That remains a separate bounded pronunciation/segmentation example, not permission to remove the rhyme requirement. Three transformation-only events (3515, 3518 and 3537) already have rap evidence. They include mechanical filler or an incomplete source, so any more permissive style mode should distinguish eligibility from writing quality.

Five exact candidate families recur across review stages; one also repeats within `turn_rewrite`. Different stages can describe a retry, its final cut and later recovery of the same retained wording. This report does not equate every repeated journal event with an extra model call. Fixing a false rhyme failure should reduce needless repair of that particular source, but this rejection-only snapshot cannot establish a throughput improvement.

Some refusals remain justified or uncertain. Added “no denying”, “no complying” and “no failin’” phrases can change certainty or refusal; a blanket negation waiver would be unsafe. Event 3496 is a 5,002-character unfinished, unpunctuated draft; the retained record lacks token-limit provenance, so it is not a proven overflow. Ad events 3524 and 3535 already start from radio chatter with mismatched product context and an embedded caller-dialogue source seed. Softer tint acceptance cannot repair that upstream composition.

The read-only state sample was on and paused, with 38 ready items, 75 awaiting tint, one rewriting, 46 needing replacement and no accepted recording debt. One tint writer was active and one waiting; 35 tint admissions had deferred. No preparation booth was active at that instant. This is evidence of writing/gating debt, not proof that an available accepted recording job was being starved. Policy remained enabled, zero editorial flags allowed, no disabled gates, revision 3. No model requests, controls, votes or settings changes were made for this audit.

The bounded code follow-up corrects the exact `touch` vowel in `_rap_nuclei`. In `_rap_bars`, a three-word comma landing must rhyme with an endpoint already present in the original full-bar grouping and precede a full clause. Final short tails, unrelated fragments and shopping lists retain their original grouping. Every word stays in order; semantic checks and policy are unchanged. All 27 focused tests pass: six new regressions, seven existing bar-evidence cases, five `test_tint_rap` cases, five `test_tint_rhyme` cases and four `test_rap_only` cases. These are local test results, not deployed production improvement measurements.
