---
name: repertoire-dead-air-lcd
description: "2026-09-08 pm/evening - the rejections census fixes, the 96-hour tinted repertoire (keep_until), the SFX gap filler, the LCD that shows only what is being said, the retirement desk, the reflection, gold bars, docs/RapAssembly.md + the 🎛 view, and the evening's contract folds (the queue is the backlog); operator's standing rules"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4d1ccc38-da47-4616-a4bb-8dd39813b3f3
  modified: 2026-09-08T23:13:53.328Z
---

Operator rules stated 2026-09-08 (standing, repeat in every future change):
- "the station having silent moments is just not gonna work" - dead air is ALWAYS punctuated with SFX clips; System2 has carte blanche to change anything to keep an endless stream; keep making rounds in the background.
- "tinted rhetoric should not expire for 96 hours. Once it is rhyming, I need that stored and played and added to the repertoire."
- Pausing must BUILD the broadcast (rendered clips ready for unpause).
- The LCD must reflect what is actively playing, never a feed.

What shipped (docs/rejections-census-2026-09-08.md, docs/repertoire-and-dead-air-2026-09-08.md):
- Rejections: 974 pending = tint 458 / draft_fragment 302 / segment_brief 128 (news 113). `line_review.INFORMATIONAL_GATES` + TRIAGE_VERSION 2 (fragments etc. are notes); `ask_model` keeps a 400+ char unpunctuated `transcript_repair` reply and JSON-shaped replies; `dj_banter(own_material=True)` on news/recap/gallery/manager/fan-mail roads (no seed, no aside, no swath - the wire is the material; `speakbox_flavor` still colours lines); `tint_repair_hint` + `rhyme_options_for` ride `report["repair_hint"]` into REPAIR EVIDENCE and the per-line lesson; `line_review_supersede` at both tint accept sites.
- Repertoire: `TINTED_KEEP_SECONDS` 96 h (env), `TINTED_KEEP_ROWS` 80; `tinted_keep_until` stamps `keep_until` lazily (news excluded; crystal-off stamps nothing; old stamp stands); honoured by `stock_expires_at`, `resort_may_drop`, larder prune/restore/`alt_larder_index`, `larder_trim` (stock + repertoire; replaces `del _LARDER[:-larder_cap()]`), `larder_stock_count` (resting kept rounds don't block writing), `row_innings` (evergreen 12 for kept), pantry row ceiling (held + 300), cache purge spares kept unless `{"force": true}`. Rest (3 h), repeat_safe, `_larder_current`, caller FIFO untouched.
- Gap filler: `sfx_fill_gap(why, under_floor)` - SFX Guy prepared liner (odd turns, not under floor) else `dj_sting(force=True)` sample; rest max(9 s, sfx_gap). Hooks: `dead_air_watch` (silent > 12 s each 20 s tick), `cover_the_gap` (after continuity, before live render; last resort of incessant branch), `talk_watch` (floor held + gap past limit, sample only). Status: `/api/dj` → `dialogue_flow.gap_filler` and `dialogue_flow.repertoire` (NOT the /api/orchestrator/logic pipeline dict). After the 14:33 restart the queue read pending 586 (was 974), noted 7847.
- LCD: `desktop/renderer/lcd.js` shows current line (pan on its own clock), holds 6 s, then idle text (paused / ♪ record / off air / on air nothing said). The desktop app must be RELAUNCHED to mirror lcd.js (done 14:25 CDT-ish Windows time).

- Retirement desk (evening 2026-09-08, docs/retirement-desk-2026-09-08.md): the operator wants APPROVAL before anything rhymed leaves the cupboard, per-type rules and life timers. `retire_may(kind,row,why)` is the one gate on every deletion road (larder prune/restore via `larder_prune_why`, `larder_trim`, burn on take, and `resort_may_drop(kind,row,keep,why)` for the shelf sweeps/trim); default rule ask="tinted" (news/station_id "never"); rules in data/retire_rules.json (ask/keep_hours/innings - `tinted_keep_until` and `row_innings` read them); ledger data/retire_ledger.json; decisions remove (row leaves at once) / keep (keep_until + innings_extra); hard ceilings force with state "forced". UI: `/cupboard/retire` page (self-authenticating like /journal), `/api/retire[?summary=1]`, `/api/retire/decide`, `/api/retire/rules`; panel ⏳ tray button + count from `/api/dj` dialogue_flow.retire; Pine Box card `createDesktopRetireNotices` (bottom-left) + main.js carve-out for /cupboard/retire; LCD cupboard lines carry ⏳ life + airings + "waits". `cupboard_state` rounds carry sid/life_left/aired/innings/rhymed/retire.

- THE WIPE OF 13:54 CST: 22 kept rounds vanished because `dialogue_row_viable` treats a moved writing contract (`_larder_profile_signature` = dials + crystal + PLOT ACT) as non-viable and both larder append rebinds dropped non-viable rows silently (only the 73399 site records `reserve_row_removed`; the 46718 site records nothing). Fixed: `_larder_current` is True for a kept rhymed round; `larder_rebind_viable()` asks the desk (`larder_unviable_why`); pending rows hold their clips (`retire_pending_ids` in `pantry_spoken_for`); keep re-binds `profile`. Lesson: any predicate named "viable/current/ready" can be a deletion road when a rebind filters on it - grep `[e for e in _LARDER if` before trusting the desk's coverage.

- EVENING (rapping 24/7, docs/rapping-247-and-the-reflection-2026-09-08.md): (1) grader: under the meaning grade a proved END rhyme the source lacked = transformation (`rhyme_added`; strict unchanged) - the operator's own typed edit had been refused for "not transformed"; (2) "Approve as written" = lab kind `accept` (rejection_workbench `_execute`), `approve_replacement(..., note=)` with `basis accepted_as_written` + `wording` on the decision, `_approved.add(instance fingerprint)`, `_refresh_preferences` admits as-written grants (`by_operator`) -> `line_review_guidance` system message; button in frontend/rejection-review.js (served at /orchestrator-review/rejection-review.js, no desktop relaunch needed); (3) the reflection: `reflection_clock` (10 min tick) -> `reflection_run(kind)` (tint_model_for(kind), purpose station:reflection) -> `data/orchestrator_reflections.json` versions -> `reflection_guidance(kind)` inside `crystal_operator_refinement` (profile hash includes the version); `reflection_judge` retires a version that drops acceptance >= 10 pts; endpoints /api/orchestrator/reflection[/run]; `PromptLearningStore.recent_outcomes` added; (4) gold: `gold_fill_gap` first in `sfx_fill_gap` (dj_speak clip=/media/<name> + media_sign), `gold_harvest_entry` at `_radio_entry_rejected`, `_call_tint_strike`, `retire_remove_now`; `gold_pick(min_rest=)`, GOLD_GAP_REST 300. Lab operation status word is 'completed'.
- The analyst's reading of 584 pending cuts (scratch rej/analysis/report.md; numbers in docs/rapping-247...): 65% graded by superseded grader/roads → triage v4 `stale_grader` (line_review STALE_GRADER_VERSION 9, STALE_ROADS_BEFORE 14:33 CST); `terminal_rhymes` must NOT get `required_depth=_rap_depth` (it killed garage/mirage, steer/near, live/arrive); `_tint_formula_turn` reads caller formula lines plain; `call_novelty` compares caller turns only (`_call_caller_text`); `segment_audit(titles=)` counts wire title words; `_pronominal_one` verb whitelist for "this one looks". Accepted vs refused bars: 94% vs 58% two-bar, 9 vs 12 words/bar, landing 79% vs 19%. The writer NEVER emits ' / ' (1 of 1,184) and echoes long sources (92 of 95 "not transformed").

- RapAssembly (2026-09-08 late): docs/RapAssembly.md is THE overview document of the whole line (ideation → rooms → crystal → tint → grader → desks → recording → stores → schedule → air → covers → learning → dials → where to look → decisions); the 🎛 RapAssembly 3js view = `rapAssemblyPanel()` in the panel (PINE_3JS key "rapassembly", desktop THREEJS_VIEWS entry) over `GET /api/rapassembly` (`rap_assembly_state`, 2 s memo). Keep both current when a stage changes. The view opens inside a `pineWin` frame (1100×760 default) - a 3js view must fit its stage (camera fit from the line's width; two rows under aspect 1.45); test views headless with scratchpad `cdp-probe.js` (Node 24 WebSocket + Edge `--remote-debugging-port`, `open-rap.js`/`probe-rap.js`, an inject file can hot-swap a panel function on the live page without a deploy). `let`-declared panel globals are NOT `window.` properties.
- THE QUEUE IS THE BACKLOG (2026-09-08 ~18:00, docs/rejections-adjust-2026-09-08.md): "rejections coming up" = 197 pending of which 180 were graded before 14:33 with grader v9 already current (triage v4's `version < 9` clause cannot see road changes) - always check `last_at` vs the deploy times before treating the queue as live. Live cuts since 16:27: 2 (the machine re-asks; 309 of 404 rewrite refusals were the Gazette's paragraphs on e2b). Meaning cuts were the contract's FORM checks: negation 62/108 (rhetorical frames), names 46 (54/71 findings = the station name "Chicken Tendo Little Pine Box FM Station"; "Taut wire" openers), question 22 (inner questions). Shipped: `crystal_contract._rhetorical_negations_only` / `_question_is_inner` / `_descriptive_opener` (WordNet index.adj/adv) / `boilerplate=` → `negation_rhetorical`, `question_inner` flags; `tint_evaluate` folds them under the MEANING grade only (advisory "…was folded…"); `_tint_boilerplate()`; `_verbatim_turn_text` (240-char bound on raw transcript swaths at head/tail/#844 sites); `line_review_regrade_pending` + `regrade_once` (90 s after boot) + `POST /api/orchestrator/rejections/regrade`; `LineReviewStore.note_stale(id, status, say)` (stale_grader / read_plain). VERSION 3 / grader v9 unchanged (accept-more only).

**Why:** the operator's whole product is the illusion of an endless station; every expiry rule was written for a 90-minute cache and none knew a round was rhymed - and the operator wants the last word on every deletion of rhymed work.
**How to apply:** any new store/expiry/eviction path must consult `tinted_kept(kind,row)`; any new silence path must call `sfx_fill_gap` before rendering anything live; never let the LCD fall back to the feed. `tests/test_sfx_pool_warm.py` fails on this Windows box only because TEMP is the 8.3 short path (EHMECK~1) - run with the long TEMP to confirm. See [floor-and-paced-air](floor-and-paced-air.md), [pause-banking-and-records-underneath](pause-banking-and-records-underneath.md), [request-book-lanes-strikes](request-book-lanes-strikes.md).
