The consistent rejection snapshot ends at event **5433**. Events **3538–5433** cover 9.03 hours and contain **1,896 occurrences**, **1,192 distinct normalized source/candidate pairs**, and **620 gate/kind/source families**. Current storage contains **2,834 pending records**, six allowed and one kept. These are later counts than the screenshot. Of the new events, **283 are cuts across 183 distinct review IDs**; 1,493 are rejected rewrites, 78 record trimming, and 42 are held before recording. These are not 1,896 discarded programmes or a failure rate across every generated attempt.

Full original/candidate wording, evaluations, retained parent identity, context, event cursors and trace references are in [the JSON audit](rejection-learning-followup-audit.json). Its manifest identifies the consistent SQLite backup and complete private wire-trace JSONL. Previous audits were not overwritten. The station was on and paused in the adjacent GET sample; editorial policy remained enabled with zero allowed faults, no disabled gates, revision 3. Learning was enabled in fluid mode at revision 2. One coordinator GET timed out; the report preserves that failure rather than filling in queue numbers.

| Gate | New events | Interpretation |
|---|---:|---|
| Tint | 1,534 | Rewrites, retries and actual turn cuts; not one event per model call |
| Draft trimming | 78 | Raw model output shortened at character or sentence boundaries |
| Segment brief | 75 | 42 news and 33 ad brief refusals |
| Draft fragment | 65 | Includes 14 explicit `NONE` control responses |
| Caller contract | 43 | Whole-call structure, source and novelty refusals |
| Radio draft | 31 | Conversational richness/structure failures |
| Blend | 30 | Rejected proposals; retained original scripts remain available |
| Repetition | 20 | Replacement of repeated wording |
| Language | 9 | Includes eight clear short-English examples |
| Line quality | 7 | Near-duplicate lines at recording/air admission |
| Recording tint / tint structure | 4 | Required tint or whole-script structure incomplete |

Only 15 new events carry `technical=true`. Rhyme appears in 1,184 events, semantic preservation in 391 and insufficient transformation in 198; reasons overlap and fluid-mode reports sometimes include both a raw reason and its effective blocking explanation. The JSON retains raw and effective faults separately. Of tint events, 1,398 contain grader version 6 and fluid-mode evidence; 130 retain version 5 and six lack a version.

The largest repair loop is one short host line: “Just get to the point, man. We got records to spin.” It accounts for **446 events and 53 candidate variants**. **439 events** belong to one retained caller parent, `caller-fd715547e2`, across **115 trace scopes over 6.44 hours**. Two exact candidates recur 99 and 95 times across many separate scopes. This is sustained retry activity, not merely two journal stages describing a single failure. Seven later occurrences belong to a second parent containing the same line.

The original parent's latest retained state has **20 of 21 turns accepted** and `tint_fails=3`. The strike logic only handles a completed active `script_tinted` that fails the final caller contract. An incomplete tint returns from `ensure_entry_tinted` after updating retry time and progress, so an indefinitely failing final turn never reaches that completion-only strike bound. Its plain call has an explicit operator acceptance, which must remain respected. The appropriate repair is to preserve the twenty accepted turns and adapt or space unsuccessful work on the remaining turn; retiring the entire programme or silently overriding the operator would not fix this mechanism.

Actual slash-separated model bars are flattened into comma-separated retained candidates. For example, the trace feeding event 5323 returns the short “man / plan” pair; its retained form loses that explicit bar boundary. This is a handler problem distinct from rhyme strength, meaning or fluent writing. The independently owned correction preserves the boundaries for grading. The phone greeting “Hold on, that's line seven five six three eight. Hello?” also recurs 112 times across 41 candidates; its exact digits and answering question remain required.

Several upstream handling problems have narrow fixes:

- Track reception explicitly requests `NONE` when search results concern another song. Generic `ask_model` prose validation rejects that valid control response as a fragment before its caller can consume it. An explicit reception-result contract can recognize only that sentinel while keeping ordinary fragment checks.
- Ten trimming events, including 4928, keep a punctuation-repair instruction prefix while dropping the actual unpunctuated caller tail. The existing exact echo cleaner runs in `speakbox_harvest` after generic model trimming. Cleaning this specific raw response before trimming preserves the actual source; raw wire response and removal provenance must remain inspectable.
- Events 4046–4052 are English `Yo` greetings; event 4975 is “Hero fail, fall, die trying.” The language heuristic treats shared `yo`/`die` tokens as foreign while missing contractions or English content verbs. Positive English context can resolve these exact ambiguities without accepting actual Spanish, German or Polish sentences.
- Quantity evidence misclassifies impersonal “make one wonder” as a new counted item. It also treats “another one bleeding into…” and “one more bleeding into…” inconsistently. These are grammatical pronouns; explicit “one more ticket,” money, digits and large quantities must continue to bind. These classifier repairs do not excuse unrelated meaning drift in the same examples.

Other refusals remain useful. Ads 5305 and 5361 discuss GPU heat, static and systems while omitting the requested enamel mug or lighthouse plate. Event 5321 describes an image without the requested sale. More permissive tint grading cannot supply a missing product or truthful sale. The latest blend, 5397, drops a medical-news discussion and rewrites protected material into a generic exchange about people and systems. Its refusal protects the retained original. News brief checking itself is only lexical framing, so it does not prove story fidelity; the current example also contains unrelated inserts and claims, making a broad news-gate waiver unjustified.

Semantic subchecks still need skeptical review. Event 4310 adds “no brakes” and certainty to a source that states only how Dale would take a turn. Event 5325 adds “how we act” as the answer to a source that only rules out abilities; its stored semantic subcheck nevertheless passes. These are frozen negative cases, not templates for acceptance. A model can satisfy a rhyme detector while adding an unsupported claim.

The captured production window contains **1,428 actual wire requests**. Of these, 1,160 contain learning revision 2 and 1,313 contain the fluid instruction. This proves the instructions reached model calls; it does not prove improvement. Missing hints must be assessed by road and prompt construction time. The independent learner audit identifies incumbent-slot starvation and missing outcome feedback. No additional learning-model task was triggered by this audit.

Trace coverage is incomplete: 1,529 new events retain a trace pointer and 1,454 have a captured wire request somewhere in that scope. No new draft-fragment, draft-trimming, segment-brief, caller-contract, radio-draft or blend event retains a `lab_trace_id`. Their full wording and reports survive, but assigning a nearby unrelated model call would be false precision. The report therefore keeps code-backed upstream attribution separate from exact trace-backed tint evidence. Wire counts include successful neighbouring turns and multiple calls in a scope; they are not a denominator of unique rejected attempts.

[The frozen evaluation manifest](rejection-learning-followup-cases.json) contains twelve distinct sources from eleven retained parents: six development cases and six holdouts, with no parent crossing the split. All twelve include complete scoped trace records, original/candidate wording, style context and historical evaluations. Caller short lines and greeting share one development parent; the holdouts include distinct gallery, news, manager, banter and SFX material plus genuine semantic negatives. No new model requests, settings, editorial decisions, recordings, playback or device controls were made for this audit.

The subsequent authorized local implementation adds explicit reception/transcript result contracts, short-English evidence and the narrow impersonal/indefinite-one corrections. It preserves existing `one more` idiom handling and does not broaden the number parser into a general grammatical classifier. Eleven new tests plus the existing source, model-output and quantity-contract tests pass: 44 checks in 0.021 seconds, with all model requests mocked.

A separate durable `tint_retry_budget` now bounds unchanged coverage debt. Four actual tint model responses without a higher accepted-turn count cause a five-minute yield, then ten, twenty and at most thirty minutes if stagnation continues. Actual completed work before a later admission deferral counts; a zero-work admission refusal or cached regrade does not. Both entry and raw-shelf wrappers preserve all original words, candidates, operator decisions and media. Existing selectors skip a waiting parent so another eligible parent can run. The context key binds original source, kind, profile, prompt, relevant strategy text/IDs, caller identity and policy; an unrelated road's learning revision does not release the wait. Explicit reviewer recovery does. Status reports the reason, remaining wait, accepted high-water and response counts. Nine isolated regressions pass, including a real selector choosing another parent, restart serialization, unchanged-candidate churn, increased accepted coverage and child-task response attribution. These are implementation checks, not yet postdeployment improvement measurements.
