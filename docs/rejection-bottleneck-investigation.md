# Rejection and recording investigation

Read-only observations on 2026-09-07, 16:00–16:05 UTC. No votes, model calls, recordings, playback changes or restarts were requested. The [JSON evidence](rejection-bottleneck-investigation.json) contains sanitized counts, trace IDs and timings.

The show was playing and had accepted recording debt with idle sampled booths. At 16:03 the pipeline held 154 items: 21 ready, 80 awaiting or undergoing tint, eight awaiting recording, and 45 requiring replacement. The four-hour call sheet independently selected five accepted rounds with 29 missing lines (415.1 seconds). Several snapshots showed zero active voice renders despite an open preparation window; these snapshots alone do not establish zero production between observations.

The take counter was misleading without its source breakdown: all 56 process takes were shelf serves. All 40 recent receipts had `how=shelf` and zero render milliseconds. Its callsites cover playback preparation only, excluding `larder_prepare` and `prep_render_line`. Counter growth therefore demonstrated playback reuse of existing audio; it cannot establish whether preparation produced new audio. Meanwhile the live stream completed real 7/7 and 11/11 line receipts; its measured talk gap was under a second. Recording debt, sampled idle booths and currently functioning playback are separate findings. Follow-up measurement uses pantry/media timestamps and active preparation ownership rather than this counter alone.

The strongest code-level scheduling candidate is in `pantry_keeper`. Its raw ad/station-ID pass awaits `ensure_shelf_row_tinted` before reaching the accepted dialogue `recording_sitting`. Its later round visits call `larder_prepare`, whose first operation can itself await a tint writer. Thus a recording pass can wait behind costly writing despite accepted work already being available. A minimal regression should block the first untinted ad's writer, place an accepted committed dialogue behind it, and prove that the recorder is reached without completing that writer. Selection must continue to exclude off-brief, technical, withdrawn or uncommitted rows. Root owns implementation and validation of this proposed repair.

Over the preceding 30 minutes, the journal contained 242 rejection events across 185 review IDs, of which 33 were actual cuts. Intermediate batch rewrites accounted for 126 events. Recorded reason occurrences included 161 semantic-preservation failures, 138 basic rhyme failures, 18 insufficient transformations and one prohibited phrase copy; one event can have multiple reasons. These are recorded refusals, not the denominator of all generated candidates, so they are not a model rejection rate.

The current crystal settings are hold enabled, coverage 100%, strength 0.88, and meaning grade. Meaning grade makes the strict internal/multisyllabic rhyme proof advisory, but a separate basic-rhyme condition still rejects a line with no rhyme evidence. Advising that rhyme is optional would be incorrect. Semantic checks use lexical/entity/question/negation evidence, and the evaluator explicitly describes its limits as conservative screening rather than phonetic or semantic proof.

The 31b writer made 17 whole-round calls with median elapsed/service/wait times of 85.0/48.2/16.6 seconds, and 38 turn calls with medians of 23.2/11.9/8.6 seconds. The turn waiting-time 95th percentile was 64.9 seconds. Live admission was bounded to one active and one waiting 31b tint job; this does not reproduce the former many-minute unbounded queue. The advertised capacity ratio of 10.38 explicitly includes task queue waits and is not a measured physical hardware ceiling.

A detailed diagnostic conversation needs better attempt attribution. The retained cut carries complete original/candidate text, grade and parent context, but recent examples have no unique current model-call ID. A stored `entry.tint.prompt` can belong to the previous pass. Pipeline logs preserve prompt detail but do not provide a unique causal link and are written before the final system guidance is attached. The interface must label prior or reconstructed prompts honestly until exact final request messages, settings, output and timings are persisted with an attempt ID.

Some terminal cut candidates equal their source because `crystal_turn` returns the original after its generated repairs fail. Earlier rejected rewrites are separate evidence; source-equals-candidate at the final gate is not proof that the model never tried a rewrite. A useful refinement should inspect those actual attempts, target their measured faults, and re-run the same grade without silently lowering thresholds or waiving technical checks.
