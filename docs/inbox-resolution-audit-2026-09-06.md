# Inbox resolution audit — 6 September 2026

This report records implementation and verification evidence for the inbox requests; it does not itself close inbox records. Response evidence includes direct source sampling and stronger topical matching, and LCD evidence includes physical mode/handoff tests. The growing response-bank counts below are timestamped snapshots, not claims that the configured target is full.

| Request | Implemented behavior | Verification and limits |
| --- | --- | --- |
| **1067** | The ordinary newspaper follows the supplied reference: tall cream sheets, bundled blackletter masthead with the full station name, lead photo with white headline beside a narrow text column, ruled five-column inside pages and restrained red accents. Native PDF uses matching portrait proportions and embedded type; tabloid remains separate. | 29 newspaper Python tests and six UI checks passed. After one restart at an observed speech gap, actual 4 AM HTML/PDF previews rendered seven ordinary pages and twelve tabloid pages. All 23 images loaded in each HTML style, article data stayed unchanged, text fit the pages and the 390-pixel view stayed centered. PDF bounds/font checks and front/inside visual inspection passed. Native PDF uses its own composition and may retain unused columns. See [style evidence](newspaper-style-1067.md). |
| **1061** | Each image receives a distinct fictional city situation combining its visible subject with its own random Speakerbox passage. A bounded JSON batch writer and independently sourced fallbacks preserve variety under queue pressure. City and classified desks share each story once; printed cards and exhausted filler are no longer recycled around the page. Nested source receipts survive publication, and later tint cannot reuse another image story's sentence. | Twenty-two newspaper tests, six UI checks and the corrected twelve-image fixture passed. Final live extra `2026-09-06-13x1441` published 23 stories and 22 plates: twelve image stories, twelve distinct images/source files, 39 unique sentences, no repeated copy or presenter patter, twelve matching source hashes and nine exact passage checks. Eleven stories used seeded fallback while the writer was busy. The request was archived through the normal resolver; see [image-story evidence](newspaper-1061.md) and the dated live evidence below. |
| **1060** | Output route status follows actual per-stream destinations, mixed routes remain recoverable as Custom routes, and continuity repairs preserve accepted dialogue. A bounded resource monitor records measured memory, model ownership, writer queues and recording work; cleanup protects active jobs and unrelated services. | Five desktop route regressions and live media inspection verified advancing songs and non-looping music/DJ elements. The DGX had about 42% host memory available during the initial inspection; missing Nabu speech was not demonstrated to be memory exhaustion. Resource cleanup and final continuity deployment checks remain separate evidence. See [continuity](station-continuity-1060.md) and [DGX resource evidence](dgx-resource-audit-1060.md). |
| **1059** | Nabu's physical dial owns its level. Playback, reconnects, startup and system route restoration cannot reapply the application's saved volume. The old automatic-volume checkbox is disabled for Nabu; a deliberate Music-slider change remains a single explicit adjustment. | Seven backend regressions and the desktop ownership regression cover old opt-ins, cache resets, announcement replay, stale device metadata and explicit controls. Read-only Home Assistant checks before and after deployment retained the unmuted 84% device level, while the saved automatic-control flag became false. The current normal desktop runner contains the disabled control. See [Nabu volume evidence](nabu-volume-1059.md). |
| **1058** | Both presenters have voice/engine-specific repertoires of 64 prepared responses each: eight acknowledgments plus 56 Speakerbox-derived variants. Random swaths come directly from enabled documents in the current mind, retaining passages and provenance without gem harvesting or a model call. One separate model visit drafts each batch, with a 120-second budget. Concrete topic anchors select relevant recordings; unrelated conversation uses neutral responses. Stored rows are revalidated and unsuitable texts can be persistently retired across both voices without deleting audio. | Twenty-four response-bank tests cover model-free sampling, provenance, matching, neutral fallback, fair voices, failed-engine isolation, retained media, generic-word false matches, reserved writer admission despite voice saturation, and atomic retirement across restart. Root verified all 128 current recordings have distinct WAV hashes and a maximum duration of 5.462 seconds; provenance, cast and retirement checks passed. The 56 source variants were curated against their exact passages; automatic future drafts remain subject to content checks. |
| **1057** | Bounded parallel recording booths reconcile every script position and preserve incomplete work. Durable render and audio queues keep ordered tails until verified completion. Broadcast outcomes support neural/lexical retrieval and evidence-backed corrective priorities. Legacy tint evidence is rechecked and failed/unknown rows enter visible repair. Station model admission is capped at two jobs per model, repertoire drafting at one, and duplicate preparation for the same road is coalesced. | Orchestrator, playback, hold-queue and tint-recovery tests cover admission, concurrency, duplicate positions, missing pieces, restart recovery, head-before-tail playback, stale proof, operator pins and retained takes. A real four-line audition received four ordered playback acknowledgments, and paused preparation produced another source recording. Capacity costs include queue delay; see the measured evidence below. |
| **1056** | Prepared listening responses bridge long turns in the other presenter's voice without removing original words or inventing an interruption boundary. Rejected takes invalidate only matching cache references, retain media, yield to later phrases, and receive two bounded fresh retries before suspension. | Response-bank and orchestrator integration tests cover complete original dialogue, final acknowledgments, continuation boundaries and cached bad-take recovery. Retry state survives restart; `retry_rejected=true` explicitly grants another bounded budget. Optional responses do not block actual dialogue. |
| **1055** | Newspaper desks treat depicted people, places, animals and goods as participants in the fictional city. Classifieds offer depicted goods and exclude portrait subjects from sales; source selection survives photo-display deduplication. | Newspaper tests verify all selected subjects remain available and the sale column offers goods rather than selling portraits or generated pictures. Generated wording remains dependent on the writing model; desk fallbacks preserve selected subjects. |
| **1054** | Recent newspaper article details and provenance enter later DJ discussion context, with story rotation, cooldown and expiry. | A behavioral test verifies article-detail rotation and six-hour expiry. This establishes usable discussion context; it does not guarantee that every generated turn mentions a newspaper story. |
| **1053** | Pine Chat and compatible chat completions collect a request after “What would you like to say?”, preserve its submitted text, and file it once. Inline requests, cancellation, expiry and separate conversations are handled. | Inbox tests cover exact Unicode text, listener isolation, duplicate prevention, ordinary inbox discussion, streaming SSE completion and archival preservation. Completion archives the original request and evidence before removing only that inbox entry. |
| **1052** | The desktop discovers and identifies Quanta LCDs, streams a scrolling newspaper with current and completed dialogue or dialogue alone, navigates editions, favorites dialogue and saves samples locally. Preparing and other booth activity are labeled, with audio actions enabled only for real clips. Compatible firmware retains Quanta galleries/protocol, top-left mode switching, and USB/Wi-Fi management. | Fourteen unit checks and fourteen hidden Electron UI checks passed. Actual CYD installation retained 33 avatars; verification recorded 274 production draws without failure, four physical top-left switches, original Quanta USB interoperability and a normal Start-menu relaunch. An exact sample was saved locally. Real ASAR worker extraction passed; a full installer build was not run. See below and [LCD documentation](pine-box-lcd.md). |
| **1051** | The hourly paper scheduler requires an active, unpaused station and enabled toggle. A newly published edition produces a pulsing, clickable notice, including the first publication after an empty shelf. | Scheduler gates and first/next publication behavior are tested. Hidden Electron smoke verified the pulse and opening the notified edition. Notification follows successful publication; writing time and service availability can delay completion after the hourly start. |
| **1047** | Copy captures every newspaper sheet, including hidden pages, into one PNG and uses the desktop clipboard bridge. Page visibility is restored after success or failure. | Six newspaper UI checks and hidden Electron smoke verify complete multi-page rendering and clipboard-bridge dispatch. The smoke rendered two full sheets into an 800×2040 image without modifying the OS clipboard. Clipboard refusal downloads the PNG and reports that fallback; large editions may be scaled. |
| **1045** | A contained, accessible reader displays the original accessible article, including short paragraphs, lists, quotes, publisher metadata and structured article bodies. Redirects are checked and extraction limitations are shown. | Newspaper backend tests include reader order, short/repeated text, structured-body fallback, redirects, bounded downloads and cache aliases. Electron smoke verified the dialog, final paragraph, Escape dismissal and date. Publisher restrictions or JavaScript-only content may prevent full extraction; this is not a paywall bypass or an archive-service replica. |

## Measured evidence and remaining operational limits

- Root's final integration verification passed **249 Python tests** in the Linux deployment environment before the coordinated restart. Independent targeted runs passed **24 response-bank**, **6 hold-queue**, **12 tint-recovery**, and **18 newspaper** tests; the final newspaper run took 0.150 seconds in the container. Newspaper validation also passed **6 UI checks**, **12 earlier hidden Electron smoke checks**, and the new three-sheet image-story fixture. The LCD owner reports **14 unit and 14 UI checks** passed. Counts overlap and are not additive.
- Live coordinator verification eventually observed **46 persisted hour outcomes**, **4 indexed with `nomic-embed-text`**, and actual neural retrieval. A real recorded audition reached `ended` with **four scheduled and four acknowledged lines**, advancing through the expected sample intervals in 6.215583 seconds. No synthetic receipts were submitted. A 30-second API floor-wait limit was then added after the initial audition request outlived its HTTP timeout. These observations do not establish that all historic or future broadcasts complete.
- Live tint migration checked **325 legacy rows**: one ad was ready and **324** required repair or replacement. In the final bounded 60-second pause check, one source response finished recording and written larder rows increased from seven to eight; the station was restored to its prior unpaused state. Whole-program ready audio remained **107.8 seconds**, and full-round tint repair was not demonstrated. Sustained fresh reserve growth and a full three-hour usable reserve remain unproven.
- The final preparation pass reached **64 ready DJ clips and 64 ready cohost clips**, each including **56 source-derived responses**. Root verified **128 distinct actual WAV hashes**, a maximum duration of **5.462 seconds**, exact provenance, the current cast and persistent retirement. The curated 56 variants were reviewed against their source passages, including corrections for unsupported assumptions and vague questions. Earlier independent checks also confirmed exact source passages after timestamp stripping and real HTTP audio-range responses for both voices. Previously flagged awkward automatic drafts are retired without deleting media; this report does not certify future automatic drafts or claim that file validation substitutes for listening to every take.
- The schedule requested about **3,040 seconds of speech per hour** and the old coordinator estimated approximately **24,430 seconds of preparation time** (about **6.79×** the available hour). Read-only review subsequently proved this estimate includes model-queue waiting in each producer's wall-time cost. Live jobs waited several minutes for seconds of model compute. This is a congested-pipeline estimate, not a measured physical hardware ceiling; queue admission and corrected cost evidence require final verification. Preserved media is also not automatically usable reserve: legacy tint checks found many old takes lacked valid current proof. Repair remains visible and retains the audio; see [the orchestrator audit](orchestrator-audit-1057.md).
- The LCD owner reports final USB and Wi-Fi firmware installation on the actual 320×240 CYD, retaining the partition layout and all 33 avatar animations after a full original-flash backup. Four physical top-left taps switched between avatar and Pine modes, and production streaming reached **274 draws with zero failures**. The original Quanta USB bridge opened concurrently, identified the same device and 33 galleries, and exited cleanly. A normal Start-menu relaunch restored the correct device/IP and produced another 18 acknowledged frames in eight seconds. An exact **11.03-second, 96,716-byte** audio sample was saved in the remembered sample folder. Final UI checks verify that current `speaking_now` text appears before its completed-history entry and that stream turns use the server clock; station data polls once per second, and preparation activity is labeled separately. Real `app.asar` worker extraction passed; a full installer build was not run.
- The newspaper reader and image-copy smoke used fixtures and a clipboard shim. The image-story fixture uses real image descriptions and source passages, with no live station writes. The separate actual post-deployment publication is recorded below; repair progress and scheduler/resource measurements remain observations rather than deductions from tests.

Read-only review found no additional tint-recovery or direct-source sampling blocker. LCD review findings were fixed and physical checks completed. Writer admission fixes address empty caller shelves starting duplicate preparation tasks and a global two-slot semaphore hiding unlimited waiters behind a one-slot model lane; final live checks remain. Newspaper desks themselves already run sequentially; a defensive writer semaphore also preserves that property for direct concurrent callers.

## Post-restart live evidence (6 September 2026, afternoon)

The "last restart" the table above waited on happened at 12:51 local time, and it
exposed the one thing the batch had not measured: the station aired no dialogue
afterwards. With the #1057 contract in force, `GET /api/tint` showed 144 lines
offered to the crystal and one passed; 324 of 325 stored rounds were marked
`repair_required`; every phone call ended "NEVER MADE AIR"; `dialogue_flow.ready`
sat at 0 of 12 while the emergency host read filler. The contract was gating
readiness, the recording gate and the pre-record door on an evaluator that
almost nothing passes. #1063 adds `crystal_tint_hold` (default off): the tint
still runs on every road and a passing rewrite is used, but a failed rewrite
airs as written and a recorded round is READY on its audio. The repeat ledger
(`line_prints`) is also cached by mtime; it was the pulse's top blocker.

Measured after the #1063 restart (13:38 local): 114 tests green in the container
(the new `tests/test_tint_yield.py` plus the tint contract, tint recovery and
schedule readiness suites); `dialogue_flow.ready` 3 of 12 within 90 seconds and
4 shortly after; 10,307 seconds of finished audio buffered; real call turns on
air every 20 to 40 seconds; the Nabu delivery verified at `audible_volume 1.0`;
writers `deferred: {}`; the pulse's only stalls were `_load_vectors` at boot.
The pipeline log since the restart holds no "recording held" or "NEVER MADE AIR"
lines and one "(#1063) went out as written" note.

Newspaper #1061, live through the normal print API after the restart: extra
`2026-09-06-13x1441` printed 23 stories with 22 plates; its image desk carried
12 image stories over 12 distinct images and 12 distinct source files, 39 unique
sentences, zero repeated sentences on the page, no presenter patter, and all 12
provenance hashes matched (9 exact passages). The writer was busy, so 11 of 12
stories used the seeded local fallback. Evidence in
`data/paper_verification/1061/2026-09-06-13x1441`. #1061 was resolved through
the resolver tool with that note.

## Reference newspaper deployment — 7 September 2026

Request #1067 was deployed after two observed quiet speech samples and verified
through the actual HTML/PDF APIs using the existing 4 AM edition. Its 23 article
records remained unchanged; both styles loaded every image, and the ordinary
view uses the supplied reference's tall cream pages, blackletter masthead,
photographic lead and ruled columns. The ordinary PDF also uses portrait
proportions and embedded type. Actual browser/PDF bounds and visual checks
passed; see [the style evidence](newspaper-style-1067.md). Final integration
verification passed **344 Python tests in 34.243 seconds** and six newspaper UI
checks. #1067 was archived through the normal resolver, which reported no
remaining inbox records. The previous #1061 archive was left intact.
